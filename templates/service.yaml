apiVersion: v1
kind: Service
metadata:
  name: {{ include "cluster-issuer.fullname" . }}
  labels:
    {{- include "cluster-issuer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cluster-issuer.selectorLabels" . | nindent 4 }}
