apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cluster-issuer.fullname" . }}-test-connection"
  labels:
    {{- include "cluster-issuer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cluster-issuer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
